/* create interface with properties and data type to use in AccountComponent */
export interface Account{
    acctype: string,
    accTypeDesc: null,
	productType: string,
	accNumber: string,
	accStatus: string,
	amount: number,
	currencyCode: string 
};